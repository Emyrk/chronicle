# Chronicle videos

Remotion Studio project for Chronicle feature demos and in-app panel lessons. Compositions live with the frontend features they explain and are registered together in `src/Root.tsx` for authoring and preview.

## Commands

```console
npm install
npm run dev
npm run lint
npm run build
npx remotion still src/index.ts <composition-id> out/frame.png --frame=0
```
