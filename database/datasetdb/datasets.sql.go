// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: datasets.sql

package datasetdb

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const deleteDataset = `-- name: DeleteDataset :exec
DELETE FROM datasets WHERE id = $1
`

func (q *querier) DeleteDataset(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteDataset, id)
	return err
}

const getDataset = `-- name: GetDataset :one
SELECT id, name, slug, wow_version, build_version, description, created_at, updated_at FROM datasets WHERE id = $1
`

func (q *querier) GetDataset(ctx context.Context, id uuid.UUID) (Dataset, error) {
	row := q.db.QueryRow(ctx, getDataset, id)
	var i Dataset
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Slug,
		&i.WowVersion,
		&i.BuildVersion,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getDatasetBySlug = `-- name: GetDatasetBySlug :one
SELECT id, name, slug, wow_version, build_version, description, created_at, updated_at FROM datasets WHERE slug = $1
`

func (q *querier) GetDatasetBySlug(ctx context.Context, slug string) (Dataset, error) {
	row := q.db.QueryRow(ctx, getDatasetBySlug, slug)
	var i Dataset
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Slug,
		&i.WowVersion,
		&i.BuildVersion,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertDataset = `-- name: InsertDataset :one
INSERT INTO datasets (id, name, slug, wow_version, build_version, description)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, name, slug, wow_version, build_version, description, created_at, updated_at
`

type InsertDatasetParams struct {
	ID           uuid.UUID `db:"id" json:"id"`
	Name         string    `db:"name" json:"name"`
	Slug         string    `db:"slug" json:"slug"`
	WowVersion   string    `db:"wow_version" json:"wow_version"`
	BuildVersion int32     `db:"build_version" json:"build_version"`
	Description  string    `db:"description" json:"description"`
}

func (q *querier) InsertDataset(ctx context.Context, arg InsertDatasetParams) (Dataset, error) {
	row := q.db.QueryRow(ctx, insertDataset,
		arg.ID,
		arg.Name,
		arg.Slug,
		arg.WowVersion,
		arg.BuildVersion,
		arg.Description,
	)
	var i Dataset
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Slug,
		&i.WowVersion,
		&i.BuildVersion,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listDatasets = `-- name: ListDatasets :many
SELECT id, name, slug, wow_version, build_version, description, created_at, updated_at FROM datasets ORDER BY name
`

func (q *querier) ListDatasets(ctx context.Context) ([]Dataset, error) {
	rows, err := q.db.Query(ctx, listDatasets)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Dataset
	for rows.Next() {
		var i Dataset
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Slug,
			&i.WowVersion,
			&i.BuildVersion,
			&i.Description,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateDataset = `-- name: UpdateDataset :one
UPDATE datasets SET
    name = COALESCE($1, name),
    slug = COALESCE($2, slug),
    wow_version = COALESCE($3, wow_version),
    build_version = COALESCE($4, build_version),
    description = COALESCE($5, description),
    updated_at = now()
WHERE id = $6
RETURNING id, name, slug, wow_version, build_version, description, created_at, updated_at
`

type UpdateDatasetParams struct {
	Name         pgtype.Text `db:"name" json:"name"`
	Slug         pgtype.Text `db:"slug" json:"slug"`
	WowVersion   pgtype.Text `db:"wow_version" json:"wow_version"`
	BuildVersion pgtype.Int4 `db:"build_version" json:"build_version"`
	Description  pgtype.Text `db:"description" json:"description"`
	ID           uuid.UUID   `db:"id" json:"id"`
}

func (q *querier) UpdateDataset(ctx context.Context, arg UpdateDatasetParams) (Dataset, error) {
	row := q.db.QueryRow(ctx, updateDataset,
		arg.Name,
		arg.Slug,
		arg.WowVersion,
		arg.BuildVersion,
		arg.Description,
		arg.ID,
	)
	var i Dataset
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Slug,
		&i.WowVersion,
		&i.BuildVersion,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
