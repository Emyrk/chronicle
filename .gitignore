# Built binaries (anchored to repo root so they don't shadow the
# same-named source dirs cmd/chronicled/ and scripts/dbcdata/).
/chronicled
/dbcdata
/logarchive
services/saffron
# Common ignore patterns, these rules applies in both root and subdirectories.
.DS_Store
.eslintcache
.gitpod.yml
.idea
**/*.swp
gotests.coverage
gotests.xml
gotests_stats.json
gotests.json
node_modules/
vendor/
yarn-error.log
.run-secret

# Test output files
test-output/

# VSCode settings.
**/.vscode/*
# Allow VSCode recommendations and default settings in project root.
!/.vscode/extensions.json
!/.vscode/settings.json
# Allow code snippets
!/.vscode/*.code-snippets

# Front-end ignore patterns.
.next/
site/build-storybook.log
site/coverage/
site/storybook-static/
site/test-results/*
site/e2e/test-results/*
site/e2e/states/*.json
site/e2e/.auth.json
site/playwright-report/*
site/.swc

# Make target for updating generated/golden files (any dir).
.gen
.gen-golden

# Build
bin/
build/
dist/
out/

# Bundle analysis
site/stats/

*.tfstate
*.tfstate.backup
*.tfplan
*.lock.hcl
.terraform/
!coderd/testdata/parameters/modules/.terraform/
!provisioner/terraform/testdata/modules-source-caching/.terraform/

**/.coderv2/*
**/__debug_bin

# direnv
.envrc
.direnv
*.test

# Loadtesting
./scaletest/terraform/.terraform
./scaletest/terraform/.terraform.lock.hcl
scaletest/terraform/secrets.tfvars
.terraform.tfstate.*

# Nix
result

# Data dumps from unit tests
**/*.test.sql

# Filebrowser.db
**/filebrowser.db

# pnpm
.pnpm-store/

# Zed
.zed_server

# dlv debug binaries for go tests
__debug_bin*

**/.claude/settings.local.json

/.env

# Ignore plans written by AI agents.
PLAN.md
ignoredlogs
ignoredlogsv2
importdata
.resync-completed.log
*.pprof

research/.other
research/core
research/talents.md
research/nampower-original.dll

# design-sync (claude.ai/design) — transient state; durable inputs in .design-sync/ are committed
.design-sync/sb-reference/
.design-sync/sb-reference-build.log
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/
